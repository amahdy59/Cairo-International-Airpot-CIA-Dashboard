name: Quality Gates, Automated Reviews & Deployment

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  audit-and-review:
    name: Code, Responsiveness & Accessibility Quality Gates
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Dependencies
        run: npm ci

      - name: 1. TypeScript Strict Typecheck
        run: npm run typecheck

      - name: 2. ESLint Static Analysis
        run: npm run lint

      - name: 3. Automated Vitest Suites
        run: npm run test

      - name: 4. Automated Reviews (Code, Responsiveness, WCAG 2.2 AAA)
        run: npm run review

      - name: 5. Production Vite Build
        run: npm run build

      - name: Setup GitHub Pages
        if: github.ref == 'refs/heads/main'
        uses: actions/configure-pages@v4

      - name: Upload Pages Artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    name: Deploy to GitHub Pages
    needs: audit-and-review
    if: github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Pages Site
        id: deployment
        uses: actions/deploy-pages@v4
